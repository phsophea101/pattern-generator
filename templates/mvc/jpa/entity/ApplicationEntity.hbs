package {{package}}.{{lowerCase service}}.biz.entity;

import com.wingmoney.data.model.RecyclableEntity;
import com.wingmoney.data.model.hibernate.AuditAutoGenerateEntity;
import lombok.Getter;
import lombok.Setter;
import javax.persistence.*;

@Setter
@Getter
@Entity
@Table(name = {{titleCase service}}Entity.TABLE_NAME)
public class {{titleCase service}}Entity extends AuditAutoGenerateEntity<Long> implements RecyclableEntity<String> {
    public static final String TABLE_NAME = "{{snakeCase service}}";
    
    private String platform;
    private String status;
}
