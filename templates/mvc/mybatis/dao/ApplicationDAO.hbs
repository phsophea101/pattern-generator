package {{package}}.core.dao;

import {{package}}.core.entity.{{titleCase service}}Entity;
import com.wingmoney.web.vo.request.PageableRequestVO;
import org.springframework.stereotype.Repository;
import java.util.List;

@Repository
public interface {{titleCase service}}DAO {

    void save({{titleCase service}}Entity {{titleCase service}}Entity);

    void update({{titleCase service}}Entity {{titleCase service}}Entity);

    {{titleCase service}}Entity findOne(String id);

	List<{{titleCase service}}Entity> findWithList(PageableRequestVO pageableRequestVO);

	long countRecords(PageableRequestVO pageableRequestVO);

}
